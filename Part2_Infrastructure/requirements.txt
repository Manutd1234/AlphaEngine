# AlphaEngine Trading Automation — full dependency set
# Verified on Python 3.11 – 3.14.
#
#   python -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#   uvicorn main:app --port 8000
#
# If vectorbt/numba cannot build on your platform, use requirements-core.txt
# instead — the backtester falls back to its built-in NumPy engine and every
# other feature is unaffected.

# --- gateway ---------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
jinja2>=3.1
python-dotenv>=1.0
httpx>=0.27
websockets>=12.0

# --- storage ---------------------------------------------------------------
duckdb>=0.10

# --- quant -----------------------------------------------------------------
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
vectorbt>=0.26

# --- distributed job queue (optional; in-process pool is used without a broker)
celery>=5.3
redis>=5.0

# --- tests -----------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
