// Intentionally empty: the console renders local snapshots and has no
// database-backed state yet. Add Drizzle tables here when a server-side
// connector actually needs to persist something.
export {};
