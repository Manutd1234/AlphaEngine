"""Pure coherence maths: money, grids, books, lattice, constraints, solver, costs.

Nothing in this package may import a driver, read a clock, or see a float.
tests/test_coherence_no_float.py enforces the last of those by reading the
source, because the rule is only worth having if it cannot be forgotten."""
