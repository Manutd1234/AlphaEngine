# Minimal, guaranteed-installable subset (no numba / no broker).
# The backtester runs on its built-in NumPy engine; everything else is identical.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
jinja2>=3.1
python-dotenv>=1.0
httpx>=0.27
websockets>=12.0
duckdb>=0.10
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
pytest>=8.0
pytest-asyncio>=0.23
