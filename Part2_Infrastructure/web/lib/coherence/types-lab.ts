/**
 * Public wire shapes for the coherence lab.
 *
 * The declarations live in two domain modules so each authored source remains
 * below the repository's length ceiling. This barrel keeps the original import
 * path stable for panes, routes, and tests.
 */

export * from "./types-lab-markets";
export * from "./types-lab-readings";
